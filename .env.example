# copilot-api Environment Configuration Example
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# GitHub Personal Access Token
# Generate using: npx copilot-api@latest auth
# Required scopes: read:user, user:email
# WARNING: Keep this secret! Never share or commit this token.
# Format: Classic tokens are 40 characters total (including 'ghp_' prefix)
# GH_TOKEN=ghp_1234567890abcdefghijklmnopqrstuvwxyz

# Server Configuration
# PORT=4141

# Account Type
# Options: individual, business, enterprise
# ACCOUNT_TYPE=individual

# Security Options
# WARNING: Enabling token display may expose credentials in logs
# SHOW_TOKEN=false

# Rate Limiting (in seconds)
# Recommended: 30-60 seconds to avoid GitHub abuse detection
# RATE_LIMIT=30

# Proxy Configuration
# Use if behind a corporate proxy
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1

# ==============================================================================
# SECURITY WARNINGS
# ==============================================================================
# 
# 1. NEVER commit this file with real tokens to version control
# 2. Use environment variables in production instead of .env files
# 3. Rotate tokens regularly
# 4. Monitor your GitHub account for suspicious activity
# 5. Deploy behind a firewall or VPN
# 6. Do not expose the API publicly without authentication
# 
# For more security information, see SECURITY_AUDIT.md
# ==============================================================================
